{
  "name": "apples-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "seed": "npm run seed -w server --silent --",
    "build": "npm run build -w server && npm run build -w web",
    "test": "npm test -w server && npm test -w web",
    "test:e2e": "npm run build && playwright test -c e2e",
    "test:e2e:only": "playwright test -c e2e",
    "start": "node server/dist/index.js",
    "mcp": "npm run mcp -w server --silent"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "concurrently": "^9.1.2"
  }
}
